const solution = require("./solution.js");

test("4, 1 should return 34", () => {
    expect(solution(4, 1)).toBe(34);
});

test("4, 2 should return 40", () => {
    expect(solution(4, 2)).toBe(40);
});